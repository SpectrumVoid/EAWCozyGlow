add_namespace = cozycoup
add_namespace = cozymain

country_event = {
	id = cozycoup.1
	immediate = {log = "[GetDateText]: [Root.GetName]: event cozycoup.1"}
	title = cozycoup.1.t
	desc = cozycoup.1.d
	picture = GFX_event_hearthswarming

	is_triggered_only = yes
	option = {
		name = cozycoup.1.a
		add_political_power = -15
		add_ideas =  cozy_interventionism
	}

}

country_event = {
	id = equestria.2
	immediate = {log = "[GetDateText]: [Root.GetName]: event equestria.2"}
	title = equestria.2.t
	desc = equestria.2.d
	picture = GFX_event_buffalo_relations

	is_triggered_only = yes

	option = {
		name = equestria.2.a
		add_political_power = -35
		BUF = {
			country_event = { days = 1 id = bufstate.1 }
		}
		ai_chance = { factor = 95 }
	}
	option = {
		name = equestria.2.b
		add_political_power = -250
		add_stability = -0.15
		add_war_support = -0.15
		add_named_threat = { threat = 5 name = EQS_aggresion_on_BUF }
		if = {
			limit = { country_exists = BUF }
			hidden_effect = {
				create_wargoal = { 
					type = annex_everything
					target = BUF
				}
			}
			declare_war_on = {
				target = BUF type = annex_everything
			}
		}
		BUF = {
			country_event = { days = 1 id = bufstate.2 }
		}
		hidden_effect = {
			modify_country_flag = { flag = celestia_evil_meter value = 12 }
		}
		ai_chance = {
			factor = 5
			modifier = {
				factor = 0
				OR = { is_historical_focus_on = yes has_global_flag = EQS_nice_celestia }
			}
			modifier = {
				factor = 2
				has_global_flag = EQS_evil_celestia
			}
		}
	}
}